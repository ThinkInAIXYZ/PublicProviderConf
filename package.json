{
  "name": "public-provider-conf",
  "version": "1.0.0",
  "description": "A tool to fetch and aggregate AI model information from various providers",
  "main": "dist/cli.js",
  "bin": {
    "public-provider-conf": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js",
    "clean": "rm -rf dist",
    "test:core": "ts-node src/test-core.ts",
    "test:build": "bash test-build.sh"
  },
  "keywords": ["ai", "models", "providers", "cli", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "typescript": "^5.2.0",
    "ts-node": "^10.9.0"
  }
}